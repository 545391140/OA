# 技术架构文档

## 📐 整体架构

本项目采用 **前后端分离** 的架构模式，遵循 **MVC (Model-View-Controller)** 设计模式。

```
┌─────────────────────────────────────────────────────────┐
│                     客户端层 (Client)                     │
│  ┌──────────────────────────────────────────────────┐   │
│  │         React 前端应用 (Port 3000)               │   │
│  │  - Single Page Application (SPA)                │   │
│  │  - Material-UI 组件库                           │   │
│  │  - i18next 国际化                               │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                          ↕ HTTP/HTTPS
                          ↕ RESTful API
┌─────────────────────────────────────────────────────────┐
│                     服务层 (Server)                      │
│  ┌──────────────────────────────────────────────────┐   │
│  │    Node.js + Express 后端 API (Port 3001)        │   │
│  │  - RESTful API 设计                              │   │
│  │  - JWT 身份认证                                  │   │
│  │  - 中间件处理                                     │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
                          ↕ Mongoose ODM
┌─────────────────────────────────────────────────────────┐
│                     数据层 (Database)                     │
│  ┌──────────────────────────────────────────────────┐   │
│  │            MongoDB 数据库                        │   │
│  │  - NoSQL 文档数据库                             │   │
│  │  - 支持复杂嵌套结构                              │   │
│  │  - 云数据库 (MongoDB Atlas)                      │   │
│  └──────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────┘
```

## 🎨 前端技术架构

### 核心技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| **React** | 18.2.0 | UI 框架，组件化开发 |
| **React Router** | 6.20.1 | 单页应用路由管理 |
| **Material-UI (MUI)** | 5.14.20 | 企业级 UI 组件库 |
| **Axios** | 1.6.2 | HTTP 客户端，API 请求 |
| **i18next** | 23.7.6 | 国际化框架 |
| **React i18next** | 13.5.0 | React 国际化集成 |
| **Recharts** | 2.8.0 | 数据可视化图表 |
| **Day.js** | 1.11.10 | 日期时间处理 |
| **React Scripts** | 5.0.1 | Create React App 构建工具 |

### 前端架构特点

1. **组件化设计**
   - 可复用组件 (`components/Common/`)
   - 页面组件 (`pages/`)
   - 布局组件 (`components/Layout/`)

2. **状态管理**
   - React Context API (`contexts/`)
   - 本地状态 (useState, useEffect)
   - 全局状态 (AuthContext, NotificationContext)

3. **路由管理**
   - React Router v6
   - 路由保护 (ProtectedRoute)
   - 嵌套路由

4. **国际化支持**
   - 支持语言：中文、英文、日文、韩文
   - 动态语言切换
   - 本地化存储

### 前端目录结构

```
frontend/
├── public/              # 静态资源
│   ├── index.html
│   └── manifest-*.json  # PWA 配置
├── src/
│   ├── components/      # 可复用组件
│   │   ├── Auth/        # 认证相关
│   │   ├── Common/      # 通用组件
│   │   └── Layout/      # 布局组件
│   ├── pages/           # 页面组件
│   │   ├── Travel/      # 差旅管理
│   │   ├── Expense/     # 费用管理
│   │   ├── Approval/    # 审批流程
│   │   └── TravelStandard/ # 差旅标准
│   ├── contexts/        # React Context
│   │   ├── AuthContext.js
│   │   └── NotificationContext.js
│   ├── services/        # API 服务层
│   │   └── locationService.js
│   ├── i18n/            # 国际化配置
│   │   ├── index.js
│   │   └── locales/     # 语言文件
│   ├── theme/           # 主题配置
│   │   └── modernTheme.js
│   └── utils/           # 工具函数
```

## 🔧 后端技术架构

### 核心技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| **Node.js** | 18+ | JavaScript 运行时 |
| **Express.js** | 4.18.2 | Web 应用框架 |
| **Mongoose** | 8.0.3 | MongoDB ODM |
| **JWT** | 9.0.2 | 身份认证令牌 |
| **bcryptjs** | 2.4.3 | 密码加密 |
| **express-validator** | 7.0.1 | 数据验证 |
| **Multer** | 1.4.5-lts.1 | 文件上传处理 |
| **Helmet** | 7.1.0 | 安全中间件 |
| **CORS** | 2.8.5 | 跨域资源共享 |
| **Morgan** | 1.10.0 | HTTP 请求日志 |
| **Compression** | 1.7.4 | 响应压缩 |
| **express-rate-limit** | 7.1.5 | 请求速率限制 |

### 后端架构模式

#### 1. **MVC 模式**
```
Model (模型层)
├── Mongoose Schema
├── 数据验证
└── 数据关联

View (视图层)
├── API 响应
├── JSON 格式
└── 错误处理

Controller (控制层)
├── 业务逻辑
├── 请求处理
└── 响应返回
```

#### 2. **分层架构**
```
┌─────────────────────────┐
│     Routes (路由层)       │  - 定义 API 端点
└───────────┬─────────────┘
            │
┌───────────▼─────────────┐
│  Controllers (控制层)     │  - 处理业务逻辑
└───────────┬─────────────┘
            │
┌───────────▼─────────────┐
│   Models (模型层)        │  - 数据模型定义
└───────────┬─────────────┘
            │
┌───────────▼─────────────┐
│  Database (数据库)       │  - MongoDB
└─────────────────────────┘
```

### 后端目录结构

```
backend/
├── config/              # 配置文件
│   └── database.js     # 数据库连接
├── controllers/        # 控制器（业务逻辑）
│   ├── travelStandardController.js
│   ├── expenseItemController.js
│   └── ...
├── middleware/         # 中间件
│   ├── auth.js        # JWT 认证
│   └── errorHandler.js # 错误处理
├── models/             # 数据模型（Mongoose Schema）
│   ├── TravelStandard.js
│   ├── ExpenseItem.js
│   ├── User.js
│   └── ...
├── routes/             # API 路由
│   ├── travelStandards.js
│   ├── expenseItems.js
│   └── ...
├── scripts/            # 数据库脚本
│   ├── initDatabase.js
│   └── migrateTravelStandards.js
├── utils/              # 工具函数
│   └── memoryStore.js
└── server.js           # 服务器入口
```

### API 设计

#### RESTful API 规范

```
GET    /api/resource           # 获取资源列表
GET    /api/resource/:id       # 获取单个资源
POST   /api/resource           # 创建资源
PUT    /api/resource/:id       # 更新资源
DELETE /api/resource/:id       # 删除资源
```

#### 主要 API 端点

- `/api/auth` - 身份认证
- `/api/users` - 用户管理
- `/api/travel` - 差旅管理
- `/api/expenses` - 费用管理
- `/api/travel-standards` - 差旅标准
- `/api/expense-items` - 费用项目
- `/api/travel-standards/match` - 标准匹配
- `/api/approvals` - 审批流程
- `/api/budgets` - 预算管理
- `/api/reports` - 报告分析

## 🗄️ 数据库架构

### MongoDB 数据模型

#### 核心模型

1. **User (用户)**
   ```javascript
   {
     firstName, lastName, email,
     password (加密), role,
     jobLevel, department
   }
   ```

2. **TravelStandard (差旅标准)**
   ```javascript
   {
     standardCode, standardName,
     priority, conditionGroups,
     expenseStandards, status
   }
   ```

3. **ExpenseItem (费用项目)**
   ```javascript
   {
     itemName, amount, description,
     status, parentItem, isSystemDefault
   }
   ```

4. **Travel (差旅申请)**
   ```javascript
   {
     userId, destination, startDate,
     endDate, purpose, status
   }
   ```

5. **Expense (费用)**
   ```javascript
   {
     travelId, category, amount,
     date, receipt, status
   }
   ```

### 数据库索引

```javascript
// 性能优化索引
TravelStandardSchema.index({ priority: -1 });
TravelStandardSchema.index({ status: 1 });
TravelStandardSchema.index({ 'conditionGroups.groupId': 1 });
ExpenseItemSchema.index({ itemName: 1 }, { unique: true });
ExpenseItemSchema.index({ status: 1 });
```

## 🔐 安全架构

### 认证与授权

1. **JWT 认证**
   - Token-based 身份认证
   - 无状态认证
   - Token 过期管理

2. **密码安全**
   - bcryptjs 加密
   - 密码强度验证
   - 防止暴力破解

3. **CORS 配置**
   - 白名单机制
   - 环境隔离（开发/生产）
   - 凭证支持

### 安全中间件

- **Helmet**: HTTP 安全头
- **express-rate-limit**: API 限流
- **express-validator**: 输入验证
- **Multer**: 文件类型验证

## 🌐 网络架构

### 开发环境

```
前端 (React Dev Server)
  http://localhost:3000
    ↓ proxy
后端 (Express Server)
  http://localhost:3001
    ↓ mongoose
MongoDB
  mongodb://localhost:27017
```

### 生产环境

```
用户浏览器
    ↓ HTTPS
CDN / 负载均衡器
    ↓
┌───────────┬─────────────┐
│  前端      │   后端 API    │
│ (静态文件)  │ (Express)    │
│ Vercel/   │ Railway/    │
│ Render    │ Render      │
└───────────┴─────────────┘
    ↓                    ↓
MongoDB Atlas        MongoDB Atlas
(可选：Redis 缓存)
```

## 📦 构建与部署

### 构建工具

- **前端**: React Scripts (Create React App)
- **后端**: Node.js 原生
- **容器化**: Docker + Docker Compose

### 部署方案

1. **Vercel** (前端) + **Railway** (后端)
2. **Render** (全栈)
3. **Docker** (容器化)
4. **GitHub Actions** (CI/CD)

## 🔄 数据流

### 请求流程

```
1. 用户操作
   ↓
2. React 组件
   ↓
3. Axios 请求
   ↓
4. Express 路由
   ↓
5. 认证中间件
   ↓
6. 控制器处理
   ↓
7. Mongoose 查询
   ↓
8. MongoDB 数据库
   ↓
9. 返回响应
   ↓
10. React 更新 UI
```

### 状态管理流程

```
前端状态
├── 本地状态 (useState)
├── 组件状态 (组件内部)
├── Context 状态 (AuthContext)
└── 服务器状态 (API 响应)

后端状态
├── Session (可选)
├── JWT Token
├── 数据库持久化
└── 内存缓存 (可选)
```

## 🎯 设计模式

### 前端模式

- **组件化**: 可复用组件设计
- **HOC/自定义 Hook**: 逻辑复用
- **Context API**: 全局状态管理
- **Render Props**: 数据共享

### 后端模式

- **MVC**: 模型-视图-控制器
- **中间件模式**: 请求处理链
- **依赖注入**: 模块解耦
- **工厂模式**: 对象创建

## 🚀 性能优化

### 前端优化

- 代码分割 (Code Splitting)
- 懒加载 (Lazy Loading)
- 缓存策略
- 虚拟滚动 (大数据列表)
- 防抖/节流

### 后端优化

- 数据库索引
- 查询优化
- 响应压缩 (Compression)
- API 限流
- 连接池管理

## 📊 技术栈总结

### 前端
- **框架**: React 18
- **UI库**: Material-UI 5
- **路由**: React Router 6
- **状态**: Context API
- **HTTP**: Axios
- **国际化**: i18next
- **图表**: Recharts
- **构建**: Create React App

### 后端
- **运行时**: Node.js 18+
- **框架**: Express.js 4
- **ORM**: Mongoose 8
- **认证**: JWT
- **验证**: express-validator
- **安全**: Helmet, CORS
- **日志**: Morgan

### 数据库
- **类型**: NoSQL (文档数据库)
- **引擎**: MongoDB
- **ODM**: Mongoose
- **部署**: MongoDB Atlas

### 开发工具
- **包管理**: npm
- **版本控制**: Git
- **CI/CD**: GitHub Actions
- **容器**: Docker
- **开发**: Nodemon, Concurrently

## 🔮 架构优势

1. **前后端分离**: 独立开发、部署、扩展
2. **RESTful API**: 标准化接口，易于集成
3. **NoSQL 数据库**: 灵活的数据结构，支持复杂嵌套
4. **组件化开发**: 代码复用，维护性强
5. **模块化设计**: 清晰的代码组织
6. **安全性**: 多层安全防护
7. **可扩展性**: 易于添加新功能
8. **国际化支持**: 多语言适配

## 📈 扩展方向

- **微服务架构**: 服务拆分
- **缓存层**: Redis 集成
- **消息队列**: 异步任务处理
- **搜索引擎**: Elasticsearch
- **文件存储**: AWS S3 / 阿里云 OSS
- **监控日志**: ELK Stack
- **API 网关**: Kong / Nginx

---

**架构版本**: v1.0  
**最后更新**: 2025-11-01

