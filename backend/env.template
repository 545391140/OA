# 携程商旅API配置
# 从携程获取API密钥后，复制此文件为 .env 并填写正确的值
CTRIP_APP_KEY=obk_rjwl
CTRIP_APP_SECURITY=eW5(Np%RrUuU#(Z3x$8@kOW(

# Amadeus API配置
# 从 Amadeus for Developers 获取API密钥
# 注册地址：https://developers.amadeus.com/
AMADEUS_API_KEY=your_amadeus_api_key
AMADEUS_API_SECRET=your_amadeus_api_secret
AMADEUS_API_ENV=test  # test 或 production

# MongoDB连接配置
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority

# Node环境
NODE_ENV=production

# 可选：强制使用测试环境（默认使用生产环境）
# CTRIP_USE_TEST_ENV=false
